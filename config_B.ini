; Config for CLIENT B — runs on VM-3
;
; Replace SERVER_IP / CLIENT_A_IP with the real IPs of your VMs.

[server]
host = SERVER_IP
port = 9000

[me]
id   = B
user = Lucy
host = 0.0.0.0
port = 9102

[peer:A]
host = CLIENT_A_IP
port = 9101
